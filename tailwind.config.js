/** @type {import('tailwindcss').Config} */
export default {
  // ... other config
  theme: {
    extend: {
      boxShadow: {
        't-lg': '0 -4px 6px -1px rgba(0, 0, 0, 0.1), 0 -2px 4px -1px rgba(0, 0, 0, 0.06)',
      },
    },
  },
  // ... other config
} 